(()=>{
const fs=()=>window.getFinalStats?getFinalStats():{str:S.str||8,agi:S.agi||8,int:S.int||8,hp:S.maxHp||120,armor:0,damage:0,crit:0,dodge:0,block:0};const clamp=(n,a,b)=>Math.max(a,Math.min(b,n));function push(c,t){c.autoLog=c.autoLog||[];if(c.autoLog[0]!==t)c.autoLog.unshift(t);c.autoLog=c.autoLog.slice(0,8)}
function start(){if(S.quest||S.bountyCombat4||S.autoMiniBattle)return toast('Es kann nur ein Auftrag gleichzeitig laufen.');const raw=16,cost=window.getEffectiveQuestCost?getEffectiveQuestCost('mini',raw):raw;if(S.al<cost)return toast(`Nicht genug Abenteuerlust. Benötigt: ${cost} AL.`);const s=fs();S.al-=cost;S.autoMiniBattle={name:'Knochenwache',hp:Math.max(65,55+S.lvl*5),maxHp:Math.max(65,55+S.lvl*5),playerHp:s.hp,maxPlayerHp:s.hp,round:1,autoLog:['Die Knochenwache erhebt sich.'],busy:false};S.screen='home';save();render()}
function step(){const c=S.autoMiniBattle;if(!c||c.busy)return;c.busy=true;const s=fs();const hit=clamp(72+Math.floor((Math.max(s.str,s.agi,s.int)-8)*1.4),55,90);if(Math.random()*100<hit){let dmg=Math.max(4,Math.floor(5+(s.damage||0)+Math.max(s.str,s.agi,s.int)*.45+Math.random()*5));if(Math.random()*100<(s.crit||0)+5)dmg=Math.floor(dmg*1.5);c.hp=Math.max(0,c.hp-dmg);push(c,`Runde ${c.round}: ${S.name} trifft für ${dmg}.`)}else push(c,`Runde ${c.round}: ${S.name} verfehlt.`);if(c.hp<=0){const gold=Math.floor(45+Math.random()*46),xp=38+S.lvl*2;S.gold+=gold;const levels=gainXP(xp);let item=null;if(Math.random()<.35&&S.items.length<S.invCap&&window.createLoot){item=createLoot('event',Math.random()<.2?'rare':'common',S.lvl);S.items.push(item)}S.quests++;S.autoMiniBattle=null;S.questResult={name:'Knochenwache',icon:'⚔️',cat:'MINIBOSS · AUTOMATISCH',gold,xp,levels,material:0,item:item?.name||null,itemRarity:item?.rarity||null,itemStats:item&&window.itemBonusText?itemBonusText(item):null};save();render();return}const armor=Math.min(.5,(s.armor||0)*.018),blocked=Math.random()*100<clamp(45+(s.block||0)+(s.dodge||0)*.5,25,80);const dmg=blocked?Math.max(1,Math.floor((7+S.lvl+Math.random()*5)*(1-armor)*.45)):Math.max(2,Math.floor((7+S.lvl+Math.random()*5)*(1-armor)));c.playerHp=Math.max(0,c.playerHp-dmg);push(c,`Knochenwache: ${blocked?'teilweise abgewehrt · ':''}${dmg} Schaden.`);if(c.playerHp<=0){S.hp=Math.max(1,Math.floor(s.hp*.4));S.autoMiniBattle=null;save();render();toast('Die Knochenwache hat dich besiegt.');return}c.round++;c.busy=false;save();render()}
function view(c){const hp=Math.round(c.hp/c.maxHp*100),pp=Math.round(c.playerHp/c.maxPlayerHp*100);return `<section class="tam4"><div class="tam4-head"><small>MINIBOSS · AUTOMATISCH · RUNDE ${c.round}</small><h1>⚔️ Knochenwache</h1></div><div class="tam4-bars"><div><b>Knochenwache ${c.hp}/${c.maxHp}</b><i><u style="width:${hp}%"></u></i></div><div><b>${S.name} ${c.playerHp}/${c.maxPlayerHp}</b><i><u style="width:${pp}%"></u></i></div></div><div class="tam4-log">${(c.autoLog||[]).map(x=>`<p>• ${x}</p>`).join('')}</div></section>`}
window.startAutoMiniBoss=start;const oldHome=window.home;window.home=function(){if(S.autoMiniBattle)return view(S.autoMiniBattle);return oldHome()};setInterval(()=>{if(S.autoMiniBattle)step()},1050);
const css=document.createElement('style');css.textContent=`.tam4{max-width:700px;margin:auto}.tam4-head,.tam4-bars{background:var(--panel);padding:12px;border-radius:14px}.tam4-head h1{font-size:19px;margin:3px 0}.tam4-head small{font-size:8px;color:var(--gold)}.tam4-bars{margin-top:8px}.tam4-bars>div{margin:8px 0}.tam4-bars b{font-size:9px}.tam4-bars i{display:block;height:9px;background:#ffffff0b;border-radius:99px;overflow:hidden;margin-top:4px}.tam4-bars u{display:block;height:100%;background:var(--danger)}.tam4-bars>div+div u{background:var(--ok)}.tam4-log{margin-top:8px;padding:9px;background:#0d0912;border-radius:10px}.tam4-log p{font-size:8px;color:var(--muted);margin:4px 0}`;document.head.appendChild(css);
})();