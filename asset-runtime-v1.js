(()=>{
'use strict';
const Arcane=window.Arcane=window.Arcane||{};
const BUILD=window.ARCANE_BUILD||document.querySelector('meta[name="build"]')?.content||'dev';
const REFRESH_KEY='arcaneAssetRefreshToken';
const cache=new Map(),preloadCache=new Map();
let refreshToken=sessionStorage.getItem(REFRESH_KEY)||'';
const params=new URLSearchParams(location.search);if(params.get('refreshAssets')==='1'){refreshToken=String(Date.now());sessionStorage.setItem(REFRESH_KEY,refreshToken);params.delete('refreshAssets');const q=params.toString();history.replaceState(null,'',location.pathname+(q?'?'+q:'')+location.hash)}
function networkUrl(src){const u=new URL(src,location.href);u.searchParams.set('assetBuild',BUILD);if(refreshToken)u.searchParams.set('assetRefresh',refreshToken);return u.href}
async function resolve(src){if(!src)return'';src=String(src);if(cache.has(src))return cache.get(src);const url=networkUrl(src);cache.set(src,Promise.resolve(url));return url}
async function bind(img,src){if(!img||!src)return img;src=String(src);if(img.dataset.arcaneAssetSource===src&&img.dataset.arcaneAssetBuild===BUILD)return img;const seq=String((Number(img.dataset.arcaneAssetSeq)||0)+1);img.dataset.arcaneAssetSeq=seq;const url=await resolve(src);if(!img.isConnected||img.dataset.arcaneAssetSeq!==seq)return img;if(img.getAttribute('src')!==url)img.src=url;img.dataset.arcaneAssetSource=src;img.dataset.arcaneAssetBuild=BUILD;img.decoding='async';if(!img.hasAttribute('loading'))img.loading='lazy';return img}
function sourceFromImage(img){const original=img.dataset.arcaneAssetSource;if(original)return original;const raw=img.getAttribute('src')||'';if(!raw)return'';try{const u=new URL(raw,location.href);if(u.origin!==location.origin)return raw;u.searchParams.delete('assetBuild');u.searchParams.delete('assetRefresh');const marker='/assets/',ix=u.pathname.indexOf(marker);return ix>=0?'assets/'+u.pathname.slice(ix+marker.length)+u.search:raw}catch{return raw}}
function hydrate(root=document.getElementById('app')){if(!root)return;const images=[];if(root.matches?.('img:not([data-arcane-asset-build])'))images.push(root);root.querySelectorAll?.('img:not([data-arcane-asset-build])').forEach(img=>images.push(img));for(const img of images){const src=sourceFromImage(img);if(/(?:^|\/)assets\//.test(src))bind(img,src)}}
function preloadOne(src){if(!src)return Promise.resolve();src=String(src);if(preloadCache.has(src))return preloadCache.get(src);const task=new Promise(done=>{const Ctor=window.__ARCANE_NATIVE_IMAGE||window.Image,img=new Ctor();img.decoding='async';img.onload=img.onerror=()=>done();img.src=networkUrl(src)});preloadCache.set(src,task);return task}
async function preload(srcs){return Promise.allSettled([...new Set((srcs||[]).filter(Boolean))].map(preloadOne))}
function criticalAssets(){const ui=window.UI_ICON_ASSETS||{},out=[ui.hp,ui.gold,ui.xp];if(typeof S!=='undefined'){if(S.screen==='home')out.push(ui.questStandard,ui.questEvent,ui.questRisk);if(S.screen==='city')out.push(ui.locationBank,ui.locationForge,ui.locationMerchant,ui.locationShrine);if(S.screen==='forge')out.push(ui.locationForge);if(S.screen==='shrine')out.push(ui.locationShrine);if(S.screen==='char'&&window.getHeroPortrait)out.push(getHeroPortrait({cls:S.cls,gender:S.gender}))}return [...new Set(out.filter(Boolean))]}
function backgroundList(){return [...new Set((window.__ARCANE_DEFERRED_PRELOADS||[]).filter(v=>typeof v==='string'))].slice(0,8)}
function scheduleBackground(){const pending=backgroundList();let index=0;const run=deadline=>{let count=0;while(index<pending.length&&count<2&&(!deadline||deadline.timeRemaining()>8||deadline.didTimeout)){preloadOne(pending[index++]);count++}if(index<pending.length){if('requestIdleCallback'in window)requestIdleCallback(run,{timeout:3500});else setTimeout(()=>run(null),700)}};setTimeout(()=>{'requestIdleCallback'in window?requestIdleCallback(run,{timeout:3500}):run(null)},3500)}
async function purgeImageCaches(){if(navigator.serviceWorker?.controller)navigator.serviceWorker.controller.postMessage('PURGE_IMAGES');if('caches'in window){for(const key of await caches.keys()){const c=await caches.open(key),reqs=await c.keys();await Promise.all(reqs.filter(r=>/\.(?:webp|png|jpe?g|gif|svg)$/i.test(new URL(r.url).pathname)).map(r=>c.delete(r)))}}}
async function forceRefresh(){sessionStorage.setItem(REFRESH_KEY,String(Date.now()));cache.clear();preloadCache.clear();await purgeImageCaches().catch(()=>{});location.reload()}
Arcane.assets=Object.assign(Arcane.assets||{},{build:BUILD,resolve,bind,hydrate,preload,forceRefresh,purgeImageCaches,get refreshToken(){return refreshToken}});window.arcaneRefreshAssets=forceRefresh;Arcane.on?.('afterRenderSettled',()=>hydrate());Arcane.on?.('bootReady',()=>hydrate());queueMicrotask(()=>hydrate());preload(criticalAssets()).finally(()=>window.dispatchEvent(new CustomEvent('arcane:critical-assets-ready')));scheduleBackground();if(refreshToken)queueMicrotask(()=>sessionStorage.removeItem(REFRESH_KEY));
})();