(()=>{
const oldUpgrade=window.fv2Upgrade;
if(!oldUpgrade)return;
const esc=v=>String(v??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m]));
const LABEL={str:'STR',agi:'AGI',int:'INT',hp:'HP',armor:'Rüstung',damage:'Schaden',crit:'Krit',dodge:'Ausweichen',block:'Block'};
function all(){return [...(S.items||[]),...Object.values(S.eq||{}).filter(Boolean)]}
function snap(){const m={};for(const it of all())m[it.id]={name:it.name,upgrade:Number(it.upgrade)||0,power:Number(it.power)||0,bonus:{...(it.bonus||{})}};return m}
function changed(before){for(const it of all()){const b=before[it.id];if(!b)continue;if((Number(it.upgrade)||0)>b.upgrade)return {it,b};}return null}
function diffBonus(b,it){const out=[];const keys=new Set([...Object.keys(b.bonus||{}),...Object.keys(it.bonus||{})]);for(const k of keys){const a=Number(b.bonus?.[k]||0),n=Number(it.bonus?.[k]||0);if(n>a)out.push({k,label:LABEL[k]||k,before:a,after:n,delta:n-a});}return out}
function popup(res){document.querySelector('.forge-result-modal')?.remove();const {it,b}=res,bon=diffBonus(b,it),powerDelta=(Number(it.power)||0)-b.power;const rows=[powerDelta>0?`<div class="fr-row"><span>Macht</span><b>${b.power} → ${it.power}</b><em>+${powerDelta}</em></div>`:'',...bon.map(x=>`<div class="fr-row"><span>${esc(x.label)}</span><b>${x.before} → ${x.after}</b><em>+${x.delta}${['crit','dodge','block'].includes(x.k)?'%':''}</em></div>`)].join('');const modal=document.createElement('div');modal.className='forge-result-modal';modal.innerHTML=`<div class="fr-card"><div class="fr-icon">🔨</div><small>AUFWERTUNG ERFOLGREICH</small><h2>${esc(it.name)} +${it.upgrade}</h2><p>Die Schmiedung hat folgende Werte verbessert:</p><div class="fr-rows">${rows||'<div class="fr-row"><span>Item-Stufe</span><b>verbessert</b><em>+1</em></div>'}</div><button onclick="this.closest('.forge-result-modal').remove()">Weiter</button></div>`;document.body.appendChild(modal)}
window.fv2Upgrade=function(){const before=snap();oldUpgrade();const res=changed(before);if(res)setTimeout(()=>popup(res),0)};
const st=document.createElement('style');st.textContent=`.forge-result-modal{position:fixed;inset:0;z-index:60;background:#000b;display:grid;place-items:center;padding:18px}.fr-card{width:min(390px,100%);background:linear-gradient(160deg,#2b2038,#17111f);border:1px solid #f4c15d55;border-radius:20px;padding:18px;box-shadow:0 20px 60px #000a;text-align:center}.fr-icon{font-size:38px;margin-bottom:4px}.fr-card>small{font-size:8px;letter-spacing:1.2px;color:var(--gold)}.fr-card h2{font-size:19px;margin:4px 0}.fr-card>p{font-size:9px;color:var(--muted);margin:4px 0 12px}.fr-rows{display:grid;gap:6px}.fr-row{display:grid;grid-template-columns:1fr auto auto;gap:8px;align-items:center;text-align:left;padding:9px 10px;border-radius:10px;background:#ffffff08}.fr-row span{font-size:10px}.fr-row b{font-size:10px;color:#ddd}.fr-row em{font-size:10px;font-style:normal;font-weight:900;color:var(--ok)}.fr-card>button{width:100%;margin-top:12px}`;document.head.appendChild(st);
})();