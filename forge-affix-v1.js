(()=>{
const A=()=>window.ARCANE_AFFIXES,B=()=>window.ARCANE_ITEM_BUILDS,ROOT='assets/icons/ui/';const RANK={common:0,rare:1,magic:2,epic:3,mythic:4,legendary:5};let selected=null,open=false;const esc=v=>String(v??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m]));const ico=(f,c='fa1-icon')=>`<img class="${c}" src="${ROOT}${f}" alt="">`;
function all(){return [...(S.items||[]),...Object.values(S.eq||{}).filter(Boolean)]}function find(id){return all().find(x=>x.id===id)}function pool(kind){return kind==='prefix'?(A()?.prefix||[]):(A()?.suffix||[])}function affDef(kind,id){return pool(kind).find(x=>x.id===id)}function rollRange(a,b,q){return Math.max(1,Math.round(a+Math.max(0,b-a)*q))}function contribution(kind,entry,q){const d=affDef(kind,entry?.id),out={};if(!d)return out;Object.entries(d.bonus||{}).forEach(([k,[lo,hi]])=>out[k]=rollRange(lo,hi,q));return out}function removeContribution(it,kind,q=Number(it.rollQuality)||.5){const e=it.affixes?.[kind];if(!e)return;const vals=contribution(kind,e,q);it.bonus=it.bonus||{};Object.entries(vals).forEach(([k,v])=>{it.bonus[k]=Math.max(0,(Number(it.bonus[k])||0)-v);if(!it.bonus[k])delete it.bonus[k]})}function addContribution(it,kind,e,q){Object.entries(contribution(kind,e,q)).forEach(([k,v])=>it.bonus[k]=(Number(it.bonus[k])||0)+v)}function rebuildName(it){const base=it.baseName||it.name;it.baseName=base;it.name=`${it.affixes?.prefix?.name?it.affixes.prefix.name+' ':''}${base}${it.affixes?.suffix?.name?' '+it.affixes.suffix.name:''}`;it.affixNameApplied=true}function pick(kind,it,exclude=[]){const p=pool(kind).filter(x=>!exclude.includes(x.id));if(!p.length)return null;const aff=B()?.infer?.(it)||it.affinity,weighted=[...p.filter(x=>x.aff===aff),...p];return weighted[Math.floor(Math.random()*weighted.length)]}function locks(it){it.affixLocks=it.affixLocks||{prefix:false,suffix:false};return it.affixLocks}function count(it){return ['prefix','suffix'].filter(k=>it.affixes?.[k]).length}function rerollCost(it){const r=RANK[it.rarity]||1;return{gold:100+r*70+(it.upgrade||0)*15,dust:5+r*4,essence:r>=4?r-3:0}}function qualityCost(it){const q=Math.round((Number(it.rollQuality)||.5)*100),r=RANK[it.rarity]||1;return{gold:90+r*55+q*2,dust:4+r*3,essence:r>=4?1:0}}function pay(c){if(S.gold<c.gold||S.forgeDust<c.dust||S.essence<c.essence)return false;S.gold-=c.gold;S.forgeDust-=c.dust;S.essence-=c.essence;return true}function eligible(it){return !!it&&it.affixV1&&count(it)>0}
function toggleLock(kind){const it=find(selected);if(!eligible(it)||!it.affixes?.[kind])return;const l=locks(it);if(!l[kind]&&Object.values(l).some(Boolean))return toast?.('Es kann nur ein Affix gleichzeitig gesperrt werden.');l[kind]=!l[kind];save?.();render?.()}function reroll(){const it=find(selected);if(!eligible(it))return;const l=locks(it),types=['prefix','suffix'].filter(k=>it.affixes?.[k]&&!l[k]);if(!types.length)return;const c=rerollCost(it);if(!pay(c))return;const oldQ=Number(it.rollQuality)||.5;for(const k of ['prefix','suffix'])if(it.affixes?.[k])removeContribution(it,k,oldQ);const q=Math.max(.22,Math.min(1,.3+Math.random()*.7));it.rollQuality=q;const used=[];types.forEach(k=>{const d=pick(k,it,used);if(d){it.affixes[k]={id:d.id,name:d.name};used.push(d.id)}});for(const k of ['prefix','suffix'])if(it.affixes?.[k])addContribution(it,k,it.affixes[k],q);rebuildName(it);save?.();render?.()}function improve(){const it=find(selected);if(!eligible(it))return;const oldQ=Number(it.rollQuality)||.5;if(oldQ>=.995)return;const c=qualityCost(it);if(!pay(c))return;for(const k of ['prefix','suffix'])removeContribution(it,k,oldQ);const q=Math.min(1,oldQ+.04+Math.random()*.05);it.rollQuality=q;for(const k of ['prefix','suffix'])if(it.affixes?.[k])addContribution(it,k,it.affixes[k],q);save?.();render?.()}
const costs=c=>`${ico('resource_gold.webp')} ${c.gold} · ${ico('resource_forge_dust.webp')} ${c.dust}${c.essence?` · ${ico('resource_forge_essence.webp')} ${c.essence}`:''}`;
function itemCard(it){const l=locks(it),sel=selected===it.id,q=Math.round((Number(it.rollQuality)||.5)*100);return `<button class="fa1-item ${sel?'sel':''}" onclick="fa1Select('${it.id}')"><div><b>${esc(it.name)}</b><small>${esc(it.slot||'Item')} · ${esc(it.rarity)} · Roll ${q}%</small><span>${it.affixes?.prefix?`${l.prefix?ico('ui_affix_locked.webp'):''} ${esc(it.affixes.prefix.name)}`:''}${it.affixes?.suffix?` · ${l.suffix?ico('ui_affix_locked.webp'):''} ${esc(it.affixes.suffix.name)}`:''}</span></div><em>${q}%</em></button>`}function detail(it){if(!it)return '<div class="fa1-empty">Wähle einen Gegenstand mit Affixen.</div>';const l=locks(it),c=rerollCost(it),qc=qualityCost(it),q=Math.round((Number(it.rollQuality)||.5)*100);return `<div class="fa1-affixes">${['prefix','suffix'].filter(k=>it.affixes?.[k]).map(k=>`<button class="${l[k]?'locked':''}" onclick="fa1Lock('${k}')"><b>${k==='prefix'?'Präfix':'Suffix'}</b><span>${esc(it.affixes[k].name)}</span><em>${ico(l[k]?'ui_affix_locked.webp':'ui_affix_unlocked.webp')} ${l[k]?'Gesperrt':'Sperren'}</em></button>`).join('')}</div><div class="fa1-actions"><button onclick="fa1Reroll()">${ico('ui_refresh.webp')} Affixe neu würfeln<small>${costs(c)}</small></button><button ${q>=100?'disabled':''} onclick="fa1Improve()">${ico('ui_forge_upgrade.webp')} Roll verbessern<small>${costs(qc)}</small></button></div>`}function panel(){const items=all().filter(eligible),it=find(selected);return `<section class="fa1-panel"><div class="fa1-head"><div><small>AHNENSCHMIEDE · VEREDLUNG</small><h2>${ico('ui_ancestral_work.webp','fa1-title-icon')} Affix-Veredelung</h2></div><span>${ico('resource_gold.webp')} ${S.gold||0} · ${ico('resource_forge_dust.webp')} ${S.forgeDust||0} · ${ico('resource_forge_essence.webp')} ${S.essence||0}</span></div><p>Forme hochwertige Beute gezielt zu deinem Build.</p><div class="fa1-list">${items.map(itemCard).join('')||'<div class="fa1-empty">Noch keine Gegenstände mit Affixen vorhanden.</div>'}</div>${detail(it)}</section>`}
function decorate(){if(S?.screen!=='forge')return;const root=document.querySelector('.fv4'),tabs=root?.querySelector('.fv4-tabs');if(!tabs)return;let btn=tabs.querySelector('.fa1-tab');if(!btn){btn=document.createElement('button');btn.className='fa1-tab';btn.textContent='Veredeln';btn.onclick=()=>{open=true;selected=null;render?.()};tabs.appendChild(btn)}tabs.style.gridTemplateColumns='repeat(4,1fr)';btn.classList.toggle('on',open);if(open){[...tabs.querySelectorAll('button:not(.fa1-tab)')].forEach(b=>b.classList.remove('on'));let p=root.querySelector('.fa1-panel');[...root.children].filter(x=>x!==root.querySelector('.fv4-head')&&x!==tabs&&x!==p).forEach(x=>x.style.display='none');if(!p){p=document.createElement('div');p.innerHTML=panel();root.appendChild(p.firstElementChild)}}else root.querySelector('.fa1-panel')?.remove()}const oldTab=window.fv4Tab;if(typeof oldTab==='function')window.fv4Tab=function(){open=false;selected=null;return oldTab.apply(this,arguments)};window.fa1Select=id=>{selected=selected===id?null:id;render?.()};window.fa1Lock=toggleLock;window.fa1Reroll=reroll;window.fa1Improve=improve;const r0=window.render;if(typeof r0==='function')window.render=function(){const x=r0.apply(this,arguments);queueMicrotask(decorate);return x};const st=document.createElement('style');st.textContent=`.fa1-icon{width:14px;height:14px;object-fit:contain;vertical-align:middle}.fa1-title-icon{width:28px;height:28px;object-fit:contain;vertical-align:middle}.fa1-panel{margin-top:7px}.fa1-head{display:flex;justify-content:space-between;align-items:center;padding:10px;border-radius:13px;background:#17101f}.fa1-list{display:grid;gap:5px}.fa1-item,.fa1-affixes button{padding:8px;background:#ffffff05;border:1px solid #ffffff0c;box-shadow:none}.fa1-affixes{display:grid;grid-template-columns:1fr 1fr;gap:5px;margin-top:5px}.fa1-affixes em{display:flex;align-items:center;gap:3px}.fa1-actions{display:grid;grid-template-columns:1fr 1fr;gap:5px;margin-top:6px}.fa1-actions small{display:block;margin-top:3px}`;document.head.appendChild(st);queueMicrotask(decorate)
})();